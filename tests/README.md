# Tests

Add tests beside each implemented data contract, factor, and scoring rule. Cover missing and stale inputs, score boundaries, and as-of behavior before adding dashboard tests. Run the suite from the repository root with `python -m pytest`.
